version: "3.9"

services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    image: fyjc-guide-web:latest
    ports:
      - "3000:80"
    restart: unless-stopped
    environment:
      - NODE_ENV=production
